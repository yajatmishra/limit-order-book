# ── Pinned Python requirements ────────────────────────────────────────────────
# Generated for Python 3.10 / 3.11 / 3.12 on Linux x86_64.
# Regenerate with:
#   pip-compile pyproject.toml --output-file requirements.txt
#
# For development (includes test + lint tools):
#   pip install -r requirements-dev.txt
#
# Install:
#   pip install -r requirements.txt

# ── Core numeric stack ────────────────────────────────────────────────────────
numpy>=1.26,<3
scipy>=1.12

# ── Data handling ─────────────────────────────────────────────────────────────
pandas>=2.2
pyarrow>=15           # Parquet I/O (read/write .parquet files)

# ── Statistical modelling ─────────────────────────────────────────────────────
statsmodels>=0.14     # OLS, GARCH helpers, unit-root tests

# ── Dashboard ─────────────────────────────────────────────────────────────────
plotly>=5.22
dash>=2.17
Flask>=3.0            # transitive Dash dependency; pinned for security

# ── Production web server (dashboard deployment) ──────────────────────────────
gunicorn>=21.2        # WSGI server used by Render / Procfile / Docker (wsgi:server)

# ── HTTP & download utilities ─────────────────────────────────────────────────
requests>=2.31
tqdm>=4.66            # progress bars for ITCH downloads

# ── Indirect / transitive pins for reproducibility ───────────────────────────
python-dateutil>=2.9
pytz>=2024.1
six>=1.16             # dateutil dependency
packaging>=24
